# Clio Memory v1.4 预览版升级清单

这是在公开版 v1.3.0 基础上整理的下一版预览。此目录不包含私人记忆、信箱正文、数据库、日志、密钥、域名、隧道地址或个人规则。

## 相比 v1.3.0 的主要变化

- 内在状态由旧版少量状态扩展为 48 项，分为情绪潮汐、关系驱力、思维回路、身体与行动四组。
- 新增复合状态推断。系统不再把单一数值直接等同于完整感受，而是由多条状态共同形成可解释状态。
- 每条状态拥有独立底色与半衰期；没有新写入时按时间回落，而不是突然归零。
- 新增念痕写入与念痕纠错。念痕只由当前使用 AI 写入或修改；修改时先撤销旧联动，再应用新内容。
- 所有有效 AI 写入与工具动作会结束旧一轮沉默，取消尚未交付的暗涌与推送候选，并从当前动作重新计时。
- 暗涌、行为与推送加入周期归属和取消闭环，避免旧窗口结果误交付给新窗口。
- 新增性格轨迹：固定人格不被一次事件改写，近 30 天反复出现的行为倾向作为柔性参考，并可查看形成证据。
- 新增心智经纬与脑上下文：把记忆、信箱、念痕、未竟、事实、内在状态和性格轨迹汇入同一个有来源的读取视图。
- 管理端增加首页聚合、念痕、暗涌、推送、性格轨迹、心智经纬、连续性、可调底色与规则等入口。
- 新增独立手机应用布局。手机端与电脑端共用后台和登录，但拥有独立导航、信息层级、管道式状态显示和底部详情抽屉。
- 管理密码支持在管理页中修改；推送名称、机器判定规则、AI 表达口吻和暗涌生成口吻可配置。

## 数据边界

- 念痕正文不允许由网页、评估模型或后台任务篡改。
- 网页只提供念痕读取；念痕修改必须由当前使用 AI 调用对应工具完成。
- 暗涌与推送保留只读审计记录，避免网页改写已经发生过的内在过程。
- 普通记忆、信箱、事实时间线、未竟和小金库仍保留各自允许的增改入口与历史版本。

## 发布前检查

- [ ] 使用新的本地测试数据库运行全部测试。
- [ ] 检查 48 项状态的当前值、底色、变化量和半衰期。
- [ ] 验证写入、修改、删除后的联动与撤销结果。
- [ ] 验证 30 分钟沉默计时会被任何有效 AI 动作正确中断。
- [ ] 验证暗涌、推送候选、已发送推送和开机交付状态。
- [ ] 验证电脑端与手机端读取同一后台数据，但使用各自独立布局。
- [ ] 验证所有管理接口均要求登录。
- [ ] 扫描并确认不存在真实密钥、私人 MCP 地址、域名、数据库、日志和用户内容。

## 公开版说明

本目录是待审核的公开预览包，不是当前私人 VPS 的直接备份。部署前必须填写自己的环境变量和配置，并先在本地或独立测试环境验证。
