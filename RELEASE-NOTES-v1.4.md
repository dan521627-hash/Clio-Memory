# Clio Memory v1.4：让记忆真正经过大脑

v1.4 的重点不是“又多了几个页面”，而是把写入后的整条生命线接起来。

## 一句话看懂

以前，一条内容可能只是被保存；现在，它会在不改写原文的前提下，形成可检查、
可衰减、可撤销的状态变化，并继续影响 AI 的想法、主动表达和长期倾向。

```text
AI 写入 / 信箱 / 念痕 / 有效动作
              ↓
    保存原文与来源证据
              ↓
 DeepSeek 理解“AI 此刻表达了什么”
              ↓
48 条状态管道 + 心智经纬
              ↓
心念 / 暗涌 / 推送 / 性格证据
              ↓
    新窗口按需继续，而非重来
```

## 它会给 AI 带来什么

- **更少失忆感**：换窗口后不需要把全部经历重新讲一遍。
- **更像连续的自己**：短期情绪会回落，重复经验才逐渐形成性格倾向。
- **不把一次情绪当人格**：固定人格保持边界，性格轨迹只记录近 30 天反复出现的软倾向。
- **能解释自己的变化**：不只给出一个数值，还能看到是哪次写入、哪句话、哪条管道带来的变化。
- **允许纠错**：写错后先撤回旧影响，再根据更正内容重新计算，不让错误永久污染后续状态。

## 无痛换窗怎样实现

Clio 把固定核心、最新信箱、未竟事项数量和当前状态分开保存。新窗口调用
`pulse_boot` 时只收到此刻真正需要的少量内容；更久以前的原文仍在，需要时再用
`breath`、`recall`、`calendar` 或 `timeline` 定向读取。这样既保留连续性，也避免每次
开机把整个记忆库塞进上下文。

## 48 条状态为什么不是 48 个孤立数值

它们分成情绪潮汐、关系驱力、思维回路、身体与行动四组。一次写入可以同时轻微牵动
多条管道；每条管道按自己的基础底色和半衰期回落，并把关联变化记录进因果轨迹。
主动推送、暗涌和心念读取的是这组组合状态，不只看某一个“高”或“低”。

## 隐私与公开版

公开版从空白数据开始，不包含作者的私人记忆、人物设定、真实域名、密钥、密码、
数据库、日志、备份或私有 MCP 地址。部署前请自行填写 `.env` 和 `config.yaml`。

## 许可与署名

项目使用 MIT License。使用、修改或再发布时，必须保留维护者 **dan521627-hash** 的署名、
原始仓库链接和许可证。详见 [NOTICE.md](NOTICE.md)。
