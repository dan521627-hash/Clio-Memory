# Clio Memory v1.4.2：推送不再藏在页面深处

这是一项小而关键的手机端修正。

当系统已经发送了尚未确认的推送时，用户不必再进入“行为与推送”页面寻找按钮：进入首页“此刻”即可直接看到确认弹层。点击“我看到了”后，这一批内容不会重复弹出；但发送内容、触发原因、等待或取消原因仍可在“行为与推送”中查阅。

它只改变“是否已读”的展示状态，不删除记忆，也不会影响暗涌、信箱或状态计算。

公开版本不包含真实记忆、人物设定、数据库、服务器地址、隧道、密钥、日志或备份。
