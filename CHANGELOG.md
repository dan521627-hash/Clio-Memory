# 更新记录

## v2.0.0 - 2026-09-07

### 2026-09-12 修正

- 清除公开包中残留的私人姓名和私人演示称呼，默认主题改为通用名称。
- 电脑端与手机端的“新增主题”按钮正式接通后台，可以自行建立大主题和子目录。
- 新写入记忆一定获得主题归属：能判断则自动归位，不能可靠判断则进入“待分类 / 需要整理”。
- 自定义子目录名称可以参与新记忆的自动归类；用户手动指定的主题仍拥有最高优先级。
- 标签继续用于搜索和辅助理解，不再被当作主题位置。
- 修复写入记忆后事实时间线不更新：可靠变化会直接形成时间线节点，不再滞留为待确认候选。

- 最近六封信改为去重、保留变化的连续事件摘要，不再显示“已完成、待确认”等系统状态标签。
- 新增并统一事实时间线、人物关系主题、关系证据和自我觉察候选。
- 新事件可联动当前状态、关系温度与养育室观察。
- 养育室自然回复与后台状态提取分离，提取失败不再吞掉回复。
- 已完成的照顾行为由后台窄范围识别，提议、疑问和计划不会误写。
- 修复手机聊天滚动、底部输入框与首页主要布局；完整 UI 重做留待下一版本。
- 公开包重新按当前 VPS 运行源码整理并执行脱敏检查。

## v1.4.2 - 2026-08-25

### 未读推送回到首页确认

- 手机端只要存在尚未确认的推送，进入“此刻”会直接弹出“我看到了”。
- 点确认后，当前这一批推送立即从首页消失，也不会再次重复弹出；推送历史仍保留在“行为与推送”中。
- 不会删除任何记忆、信箱、暗涌或推送记录，只是标记为已看见。

## v1.4.1 - 2026-08-25

### 真静默后才判断暗涌与推送

- 每一次有效的 AI 写入或操作都会立即结束旧周期，并从零开始进入 30 分钟“候静默”。
- 只有完整 30 分钟内没有新的操作，系统才进入真正静默、开始暗涌计时，并让 DeepSeek 判断是否适合推送。
- 旧周期留下的待推送、待交付和暗涌候选会被取消，避免新写入后仍出现上一轮的内容。

### 48 条状态参与判断，过程可以看懂

- 暗涌和推送会取得本轮最明显的状态牵动、48 条管道数和真实写入摘要，用它们决定时机、强度和口吻。
- 手机管理页新增“这一轮进行到哪里”：可看到本轮写入、候静默、参与判断的状态和 DeepSeek 的下一步。
- 已送达推送会显示内容与原因；等待、跳过或取消也会说明原因，不再只显示英文状态码。
- “我看到了”重新出现在手机端；点击后仅确认当前未读推送，避免同一条重复出现，不会删除记忆或历史。

### 数据边界

- 公开版本继续不携带真实记忆、数据库、人物规则、服务器地址、隧道、密钥、日志或备份。

## v1.4.0 - 2026-08-25

这一版把 Clio 从“能保存记忆”继续推进成一套可解释、可回退、会联动的外置大脑。
写入不再只落进一个桶：它会经过状态理解、心智经纬、心念、暗涌、推送与性格证据，
并把每一步的原因留给人检查。

### 48 条状态管道与可解释联动

- 内在模型扩展为 48 项状态：情绪潮汐 16、关系驱力 12、思维回路 10、身体与行动 10。
- 每条管道都有当前值、基础底色、半衰期、最近牵动、回落目标和关联管道。
- DeepSeek 只负责理解当前 AI 写入内容所表达的状态，不把使用者的情绪误写到 AI 身上。
- 状态会继续影响心念、暗涌候选、主动推送、心智经纬与长期性格证据；页面会显示“为什么变”和“牵动了什么”。

### 念痕、心念与性格轨迹

- 念痕由当前对话中的 AI 主动写入，保留当下最真实的一句话；网页和后台模型不能篡改它。
- 心念继续区分闪念、执念和已放下，并显示来源、形成原因与后续影响。
- 性格轨迹不再暴露“输出回流”等技术词，而是把重复证据翻译成可理解的性格倾向。
- 每条倾向都能查看形成原因和具体证据；一次事件不会直接改写固定人格。

### 可撤销的闭环

- 只有信箱、念痕和记忆桶写入会改变 48 条状态；未竟事项、事实时间线、小金库等事务模块只负责记录与展示。
- 任何成功的 AI 动作仍会立即打断旧沉默周期，但不会因此被误算成情绪证据。
- 如果原写入被修正，系统先撤销由旧内容产生的状态变化，再用新内容重新计算，给误写留下容错空间。
- 心智经纬根据真实写入和后续回落动态变化，不再固定显示满格。

### 沉默交接、暗涌与主动推送

- 有效 AI 动作或新写入会立即结束旧沉默周期，取消旧一轮尚未交付的暗涌和待推送结果，再从当前动作重新计时。
- 暗涌和主动推送会读取全部 48 条当前状态来决定强度、矛盾感与表达口吻，但具体内容只能来自真实信箱、念痕和记忆事件；生成结果不会反向修改状态。
- 暗涌继续遵守“沉默 30 分钟后开始等待，再按设定生成”的节奏，并记录预计生成、实际生成和交付状态。
- 主动推送保留 AI 判定，但现在会显示已发送内容、已取消或跳过的原因，不再只出现英文状态码。

### 无痛换窗

- `pulse_boot` 只交付新窗口真正需要的固定核心、最新信箱、未竟数量和当前状态，不一次读取全库。
- 旧窗口形成的交接在新窗口读取后完成交付，状态按规则回到底色；需要细节时再按日期、主题或事实调用工具。
- 开机工具提醒去掉内部密封值和解释性废话，保留可直接调用的入口。

### 管理端与数据边界

- 搜索支持关键词、人物、日期以及记忆、信箱、念痕与心念等范围。
- 记忆库按主目录、子目录、单条记忆展开；小金库、未竟事项、事实时间线、状态底色与因果详情补齐可用入口。
- 公开包继续只提供空数据库与占位配置，不包含真实记忆、人物规则、域名、密钥、日志、备份或私有 MCP 地址。
- MCP 工具名和旧参数继续兼容；`living_memory` 的展示名称更新为“心智经纬”。

这里记录 Anima 公开版的重要变化。公开版不包含任何使用者的真实记忆、
人物设定、域名、密钥、密码、数据库、日志或备份。

## v1.3.0 - 2026-08-20

这一版补齐了“记忆位于哪里、为什么此刻被想起、现在是否仍然有效”这条线，
同时把新窗口真正需要的入口说明补完整。

### LMC-5 五维活体记忆坐标

- 新增 `living_memory`：按单个桶读取 X 时间线、Y 关系网、Z 事实演化、
  E 情绪与张力、M 记忆代谢五个旁路坐标。
- 坐标只用于理解和检索，不覆盖 Markdown 原文，也不把计算结果冒充事实。
- 新增完整度与精确坐标输出，缺失的维度会明确显示，不偷偷猜满。

### 状态与人格轨迹按需查看

- 新增 `self_state`：AI 需要理解自己时，可查看此刻状态、活跃心念与近期人格轨迹。
- 新增 `personality_preview`：只读观察反复出现的偏好、修正与长期模式。
- 新增 `continuity_review`：查看不含原文和密钥的连续性事件账本，并可校验事件链。
- 这些内容不强塞进每次开机摘要，只有当前问题需要时才调用。

### 日期、事实与开机交接

- `calendar` 按北京时间查询任意日期，只呈现当天真正发生或写入的内容，
  不再把普通读取、浏览和修改动作当成生活事件。
- `timeline` 支持自然措辞和部分名称查询，不必先猜中一个精确事实名。
- 开机摘要保留近期交接能力，同时减少空栏目与无内容提示；没有新写入时不伪造“最近发生”。
- 未竟事项只提示数量，需要时再调用 `tasks` 深读，避免开机一次塞入全部待办。

### 完整工具地图

- `pulse_boot` 现在列出全部 23 个 MCP 工具，并按记忆、写入、状态、事务分组。
- 工具地图明确包含按日期查询的 `calendar`、事实变化的 `timeline`、
  五维坐标 `living_memory`、自我状态 `self_state` 和连续性检查 `continuity_review`。
- AI 被提醒只按当前问题调用，不一次读取全库；旧工具名称、参数与调用方式保持兼容。

### 测试与数据边界

- 新增 LMC-5、自我状态、连续性账本、日历、事实时间线和开机工具地图回归测试。
- 公开仓库继续只提供空壳代码与占位配置，不包含私人数据或部署凭据。
- 本次升级不迁移、不改写已有记忆正文。

## v1.2.0 - 2026-08-17

这一版把“窗口何时真正结束”和“记忆之间为什么有关”说清楚了，也修正了
长期追加后容易出现的分类漂移。

### 明确交接，不再靠沉默猜测

- 普通停顿不再启动暗涌，也不会因为窗口开着但暂时没说话而产生提醒。
- 只有写入内容明确表达归档、告别、换窗口或当天收尾时，系统才进入静默交接。
- 新窗口读取只做交付，不会顺手清空尚未处理的状态；主动推送可一次批量确认。

### 追加内容不再带偏原桶

- `hold` 与 `grow` 追加到已有桶时，继续沿用原桶的主题和分类。
- 新片段仍会参与向量、日期、状态和候选事项分析，但不会把整只旧桶改成新片段的类别。
- 返回结果使用真正写入的桶号，方便后续准确 `recall`。

### 事实变化成为一条线

- 网页端以纵向节点展示同一事实的旧值、变化日期、当前值和来源。
- 已确认的新事实优先于相似但过时的旧记忆。
- 相关记忆会标明是“事实演化”还是“语义相似”，不再只给一个模糊的相似关系。

### 检索与兼容

- 向量模型不可用时，关键词检索仍可兜底。
- 保留 v1.1.0 的非 Docker 启动、`.env` 自动读取、自定义推送名称和启动安全检查。
- MCP 工具名称与旧参数保持兼容，本次更新不改写任何记忆正文。

## v1.1.0 - 2026-08-14

这次更新重点处理了长期使用时容易遇到的误判、安全默认值和状态交接问题。

### 未竟事项更准确

- 只把有明确动作、对象或完成条件的事情识别为未竟事项。
- “以后要更温柔”“下次不惹你生气”一类关系态度、相处承诺和表达风格，
  不再被误当成待办。
- 新增服务端二次校验，即使外部模型判断过宽，也会拦住明显不属于任务的内容。
- 为任务候选补充 `task_type` 和 `completion_criterion`，便于解释它为什么是任务、
  怎样才算完成。

### 启动安全加固

- 服务启动时会检查返回验真暗语；缺失或仍为示例值时拒绝启动，并给出明确提示。
- 网页管理端不再把示例密码视为已经完成配置。
- 启动脚本仍会为第一次安装自动生成随机私密值，旧的正常配置不受影响。

### 状态提示更清楚

- 衰减引擎尚未执行首次记忆操作时，健康检查显示
  `idle_until_first_memory_operation`。
- 该状态表示“等待首次使用”，不再与真正的停止或故障混在一起。

### 暗涌交接更完整

- 确认收到一条主动推送时，只结算对应互动。
- 尚未交付给下一窗口的暗涌不会被一并清除，避免跨窗口交接内容意外丢失。

### 公开空壳与测试

- 公开示例中的名称和推送标题改为中性占位符。
- 增加启动安全、任务边界和状态交接回归测试。
- 本次更新不迁移、不改写任何现有记忆正文，MCP 工具名称和旧调用方式保持兼容。

### 网页小屋对齐修正

- 统一小屋入口文字的水平、垂直居中方式和行高。
- 情绪数值使用等宽数字，并修正标题受旧样式影响而偏向一侧的问题。
- 按背景画面中的真实牌匾逐一校准书房、信箱与未竟事项的位置，手机页面不再出现
  “透明按钮居中、文字却没有落在牌匾中央”的情况。

### 非 Docker 安装兼容

- 补齐管理页需要的 `fastapi`、`uvicorn`、`pyzipper` 与 `.env` 读取依赖。
- `manager_server.py` 现在可以直接用 `python manager_server.py` 启动。
- 本地运行时会自动读取 `.env`，不再要求用户先手动导入环境变量。
- `breath` 的检索提示明确要求 `recall` 同时携带桶号与检索编号，避免把
  `retrieval_id` 误当成 `bucket_id`。
